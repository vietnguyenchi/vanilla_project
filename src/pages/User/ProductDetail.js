import { header } from "../../components/header";

export const ProductDetail = (product) => {
    return /*html*/`
        ${header()}
        <div id="productDetail" class="container mt-5">
            
        </div>
    `;
}